/* exported head */
/**
 * PSEUDOCODE:
 *
 * define a function 'head', that takes one argument 'array' an array of unknown type, which returns
 * an unknown type.
 *  return the value of the first index of 'array'
 */
function head(array: unknown[]): unknown {
  return array[0];
}
