'use strict';
/* exported getFirstChar */
/**
 * PSEUDOCODE:
 *
 * Define a function, named 'getFirstChar', that accepts one argument 'string' of type string, and returns a string.
 *  return the value of the first letter of the word
 */
function getFirstChar(string) {
  return string[0];
}
